# build output
dist/
.astro/

# dependencies
node_modules/

# source crawl (원본 HTML 참고용 — 저장소에 올리지 않음)
_source/

# ralph loop sentinel (로컬 전용)
RALPH_DONE

# logs / env
*.log
.env
.env.*
.DS_Store

# 빌드 시 인스타 API로 내려받는 이미지 (매 빌드마다 재생성)
public/instagram/
